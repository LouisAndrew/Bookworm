import React from 'react'
import styled from 'styled-components'

const Rev = ({ username, imgUrl, rev, bookName }) => {
      return (
            <Container>
                  
            </Container>
      )
}

export default Rev

const Container = styled.div`
      padding: 5%;
      display: flex;
`